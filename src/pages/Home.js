import React, { useRef } from "react";
import "./Home.css";

const Home = () => {
  const sectionRefs = {
    about: useRef(null),
    pastEvents: useRef(null),
    ourTeam: useRef(null),
    contact: useRef(null),
  };

  return (
    <div></div>
  );
}

export default Home;
